{{outlet}}

<div id="welcome">
        <div id="trigger">{{fa-icon "envelope" style="color: #FFF; background-color:rgb(36,104,255); border-radius: 10px; padding: 10px;"}}</div>
        <h1 class="display-4" style="font-weight: 600;">Configure Action</h1>
        <p class="text-muted">Step 2 of 6</p>
</div>

{{!-- <form class="form-horizontal" {{action "saveEmail" on="submit"}} novalidate>
    <div class="form-group">
        <label for="emailADR" class="control-label">Email Address</label>
        {{input type="text" id="emailADR" value=emailADR class="form-control"}}
    </div>
    <div class="form-group">
        <button class="btn btn-light">Continue</button>
    </div>
</form> --}}

{{#validated-form
  model        = (changeset model UserValidations)
  on-submit    = (action "saveEmail")
  as |f|}}
 
  {{f.input label="Input Email Address" type="email" id="emailADR" name="emailADR"}}

  {{f.submit label="Continue" disabled=f.loading}}
{{/validated-form}}