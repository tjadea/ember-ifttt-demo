{{!-- IFTTT when feed is chosen, link to list of actions --}}
    <main class="container">
        <h1 class="leading-text">if
            {{fa-icon "rss" style="color:#FFF; width: 25%; background-color:rgb(237,117,5); border-radius: 10px; padding: .5rem"}}
            then
            <a href="if-feed-then-action">
                {{fa-icon "plus-square"}} that
            </a>
        </h1>
    </main>